

CREATE TABLE user (
	user_id INTEGER, 
    username VARCHAR(10),
    user_name VARCHAR(20), 
    address VARCHAR(20), 
    phone LONG,
    email VARCHAR(30), 
    user_password VARCHAR(20),   
    PRIMARY KEY (user_id)
);
